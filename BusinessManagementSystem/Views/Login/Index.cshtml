@model BusinessManagementSystem.Dto.LoginRequestDto
@{
    var loginResponse = (BusinessManagementSystem.Dto.ResponseDto<BusinessManagementSystem.Dto.LoginResponseDto>)ViewData["LoginResponse"];
    Layout = "_Login";
}



@* <div class="container">
    <div class="frame-login">
        <div class="nav">
            <img class="header" src="~/images/cotiviti_logo_short.jpeg" />
            <h4 class="text-center" style="color:white">Freak Street Empire</h4>
        </div>
            <form class="form-signin" method="post" asp-controller="Login" asp-action="LoginUser">
                <div class="form-group">
                    <label asp-for="Username"></label>
                    <input asp-for="Username" class="form-styling" />
                    <span asp-validation-for="Username" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Password"></label>
                    <input asp-for="Password" type="password" class="form-styling" />
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>
                <div class="btn-animate">
                    <button class="btn-signin">Login</button>
                    <br />
                    @if (loginResponse != null)
                    {
                        if (loginResponse.StatusCode != System.Net.HttpStatusCode.OK)
                        {
                                <br />
                            <div class="text-danger">@loginResponse.Message</div>
                        }
                        else
                        {
                             <br />
                            <div class="text-danger">@loginResponse.Message</div>
                        }
                    }
                </div>
                <br />
                <div class="pt-4 p-2 m-5">
                <span class="text-white">Don't have account? </span><a asp-action="Register">Register</a>
                </div>
            </form>
    </div>
</div> *@

<div class="container">
    <div class="wrapper">
        <form method="post" asp-controller="Login" asp-action="LoginUser">
            <div class="login is-active">
                <div class="profile"></div>
                <div class="form-element">
                    <span><i class="far fa-envelope"></i></span><input asp-for="Username" placeholder="Email/Username/Phone Number" />
                </div>
                <div class="form-element">
                    <span><i class="fa fa-lock"></i></span><input type="password" asp-for="Password" placeholder="Password" />
                </div>
                <span asp-validation-for="Username" class="text-danger"></span>
                <span asp-validation-for="Password" class="text-danger"></span>
                <button class="btn-login">Login</button>
            </div>
        </form>
        <form method="post" asp-controller="Login" asp-action="RegisterUser">
            <div class="register down">
                <div class="form-element">
                    <span><i class="fa fa-user"></i></span><input  type="text" placeholder="Full Name" />
                </div>
                <div class="form-element">
                    <span><i class="fa fa-envelope"></i></span><input type="email" placeholder="Your Email Address" />
                </div>
                <div class="form-element">
                    <span><i class="fa fa-lock"></i></span><input type="password" placeholder="Password" />
                </div>
                <div class="form-element">
                    <span><i class="fa fa-lock"></i></span><input type="password" placeholder="Re-Enter Password" />
                </div>
                <button class="btn-register">register</button>
            </div>
        </form>
        <div>
            @if (loginResponse != null)
            {
                if (loginResponse.StatusCode != System.Net.HttpStatusCode.OK)
                {
                    <br />
                    <div class="text-danger">@loginResponse.Message</div>
                }
                else
                {
                    <br />
                    <div class="text-danger">@loginResponse.Message</div>
                }
            }
        </div>
        <div class="login-view-toggle">
            <div class="sign-up-toggle is-active">Don't have an account? <a href="#">Sign Up</a></div>
            <div class="login-toggle">Already have an account? <a href="#">Login</a></div>
        </div>
    </div>
</div>

